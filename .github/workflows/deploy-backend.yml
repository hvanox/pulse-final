name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - backend_fresh/**
      - requirements.txt
      - Dockerfile

jobs:
  build-deploy:
    name: Build & Deploy Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/pulse-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/pulse-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Раскомментируй нужный способ деплоя:

      # --- SSH / VPS ---
      # - name: Deploy to server via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       docker pull ${{ secrets.DOCKER_USERNAME }}/pulse-backend:latest
      #       docker stop pulse-backend || true
      #       docker rm pulse-backend || true
      #       docker run -d \
      #         --name pulse-backend \
      #         --restart always \
      #         -p 8000:8000 \
      #         -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
      #         -e CORS_ORIGINS=${{ vars.CORS_ORIGINS }} \
      #         ${{ secrets.DOCKER_USERNAME }}/pulse-backend:latest

      # --- Render ---
      # - name: Trigger Render deploy
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

      # --- Railway ---
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@v1
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: pulse-backend
